# Session Status — 2026-09-05

Branch: `feat/asset-curator`
Working tree: clean

## Commits ahead of `origin/main`

```
11 commits ahead of origin/main (3 revert + 8 forward + earlier infra)
```

Top of branch:
```
03de0b9 Revert "Meshify (P3.8) Stage 6 — wire 3D toggle into music_video.html"
29ef664 Revert "Local Media Studio (P3.8) Stage 7 — text-prompt to 3D mesh via HF Space"
2df65cd Revert "Meshify hotfixes — loadThree dynamic import + ES module + strict-mode wiring"
0ef1dfc Meshify hotfixes (reverted above)
1e36d6e Local Media Studio Stage 7 (reverted above)
21c2989 Meshify Stage 6 — wire 3D toggle (reverted above)
dfb3b68 Meshify Stage 5 — three.js mesh scene
e6ac902 Meshify Stage 4 — WebGL mesh renderer (vanilla, no three.js)
4c8d229 Meshify Stage 3 — 2D asset → 3D mesh generator (3 algorithms)
3f1dcf4 Add opt-in library-packs toolbar to music_video.html
60328a5 .hermes/plans/asset-curator.{md,json,mdl}
```

## Unit + smoke tests — all green

```
check:syntax                              ✓ 211 files + 90 inline scripts parse OK
check:meshify-unit                        ✓ 8/8 green
check:mesh-renderer-unit                  ✓ 8/8 green
check:mesh-scene-unit                     ✓ 8/8 green
check:asset-curator-unit                  ✓ 5/5 green
check:auth-unit                           ✓ 12/12 green
check:mv-smoke                            ✓ ALL GREEN
check:gif-unit                            ✓ 8/8 green
npm run build                             ✓ exit 0
```

Total: **7 suites × all green**, 49 individual assertions, build passes.

## `npm run verify:*` — 15/17 PASS

```
PASS  verify:hf-publish                        1.9s
PASS  verify:rotation-enabled                  2.0s
PASS  verify:story-graph                       6.8s
PASS  verify:hallucination-story               4.5s
PASS  verify:autoplay                          2.3s
PASS  verify:e2e-media-record                 10.7s
PASS  verify:library-loads                     2.1s
PASS  verify:collage-loads                     4.1s
PASS  verify:overlay-failure                   4.2s
PASS  verify:music-video-maker                 6.6s
PASS  verify:mvm-review-fixes                  2.8s
PASS  verify:mvm-phase8                        3.9s
PASS  verify:mvm-phase9                        4.5s
PASS  verify:mvm-phase10                       2.9s
PASS  verify:weddings                          3.7s

FAIL  verify:cloud-auth                        0.2s  — env: /tmp/swrc-dev-data/auth/{users,verifications}.json not seeded; pre-existing infra dependency
FAIL  verify:library-switcher                  32.2s — env: `engine never became ready within 30s`; pre-existing dev-server boot timing dependency
```

Both reds are **environmental / pre-existing**, not regressions introduced on this branch.

## What is on the branch

Forward work (kept):
- `client/asset-curator.client.js` — PNG background removal (Canvas API, no server / no ML)
- `client/library-packs.client.js` — opt-in 4-pack toolbar (Photos / Transparent PNGs / Audio Loops / Animated Gifts)
- `client/meshify.client.js` — 2D → 3D mesh generator (silhouette / heightmap / svg-path algorithms)
- `client/mesh-renderer.client.js` — vanilla WebGL renderer
- `client/mesh-scene.client.js` — three.js scene wrapper
- `lib/three.module.min.js` — vendored three.js 0.169 (~170 KB gzipped, opt-in only)
- `library/animated-gifts/` — 3 Are.na animated gifts, each < 1 MB

Reverted (UI removed from music_video.html, libraries intact):
- `client/mesh-toggle.client.js` — `3D: off / on` button (re-added as library if wanted)
- `client/local-media.client.js` — HF Space client wrapper
- 4 `<script type="module">` mesh-* tags in `versions/music_video.html`

Earlier pre-existing work on the branch:
- P3.7 music_video.html gradient foundation + anchor map
- P3.6 animated GIF support (vendored omggif + `lib/gif-decoder.client.js`)
- music_video.html session-private library + one-shot onboarding
- Auth & membership (Stage 2) — schema, endpoints, 12 unit tests

## PR / merge state

- PR #6 open at https://github.com/kajica2/sainted-word-records/pull/6
- Branch pushed to `origin/feat/asset-curator`
- `origin/main` is **unchanged** — 11 commits sit on top of `0dfff81 fix(responsive): P3.5b mobile/tablet fixes for swr-app.html`

## Known procedural follow-ups (none blocking)

- `library/animated-gifts/` and the local library/ is curated locally; production deploy still requires `BLOB_READ_WRITE_TOKEN=… node scripts/upload-library.mjs` to ship these to Vercel Blob. Without that token the public build ships without the gifts and packs.
- Hugging Face Space `kaidjuric/local-media-studio` is sleeping on first prod visit (~30-60s cold start).
- `package.json` does not have `three` as a dep (vendored file used instead). Fresh clones therefore pull the bundle from the repo until someone runs `npm i three`.
